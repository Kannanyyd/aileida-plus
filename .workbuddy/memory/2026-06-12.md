## 2026-06-12 补充需求实现

### 已完成的变更

1. **DB schema 新增 2 张表**：`model_strengths`（擅长方向）、`user_reviews`（结构化用户点评，10 维评分）
2. **推荐评分引擎**（packages/pricing-core/src/calculator/recommend.ts）：多目标排序公式，7 种场景权重配置，A/B/C 三档推荐方案。+7 个测试用例。
3. **审核过滤**（apps/web/lib/review-filter.ts）：关键词/规则检测，自动标记违规评论。
4. **用户点评系统**：ReviewForm（10 维评分表单） + ReviewSection（展示+筛选+免责声明） + API `/api/v1/reviews`
5. **模型推荐助手**（/recommend）：5 步向导（场景→强度→预算→技术→质量→3 档方案），模拟数据展示
6. **模型详情页增强**：新增 擅长方向/适合用户/使用建议/注意事项/风险提示/不同强度成本估算/用户点评表单
7. **首页增强**：新增"不知道怎么选？"入口、"热门使用场景"卡片；SEO 链接改为中性表达
8. **后台增强**：admin/page.tsx 重建为概览页；admin/reviews/page.tsx 用户点评审核页
9. **全局免责声明**：SiteDisclaimer 组件，前端页脚和详情页均使用
10. **导航更新**：TopNav 新增"推荐"入口

### 设计原则落地
- 所有评价使用中性表达（更适合/更适合/在当前价格下/建议结合测试）
- 禁止攻击性、绝对化表述（过滤规则 + 审核队列）
- 每条推荐结果带风险提示和数据来源说明

### 补充功能：每日 AI 动态 + 会员/Plan 体系（2026-06-12 第二次迭代）

1. **新增 8 张 DB 表**：news_sources, news_events, news_items, daily_digests, product_offerings, subscription_plans, exchange_rate_snapshots, source_fetch_logs
2. **新闻源注册表**（apps/worker/src/sources/news-registry.ts）：25+ 数据源配置（国内 9 家厂商官方公告+价格页、3 家科技媒体、4 家海外厂商、2 家开源平台、2 个政策源、2 个 GitHub 库）
3. **每日 AI 动态页**（/ai-news）：按分类筛选（10 种分类标签），展示标题/摘要/来源/影响分析/重要性
4. **会员/Plan 对比页**（/plans）：按厂商展示多档套餐（免费/Plus/Pro/Team/Enterprise），与 API token 价格严格分离
5. **首页新增"今日动态"模块**：6 类动态计数卡片（新模型/价格变化/优惠/会员/政策/能力）
6. **厂商动态页**（/providers/[slug]/news）：DeepSeek/阿里云百炼/火山方舟/智谱
7. **模型更新日志页**（/models/[slug]/changelog）：版本发布、价格调整、能力升级时间线
8. **导航新增"会员"和"动态"入口**
9. **价格体系区分**：API token 价格 / Chat 会员 / Team Plan / 企业套餐四种计费形态完全分离

### 补充功能：厂商档案系统 + 去除硬编码（2026-06-12 第三次迭代）

1. **providers 表扩展至 45+ 字段**：legal_name, brand_name, short/long_description, founded_year, headquarters, company_type, provider_category, parent_company, 12 个官方 URL, main_products/models, strengths, suitable_users, billing_features, supported_currencies, supports_api/subscription/enterprise/domestic_payment/invoice, data_source_urls, profile_confidence_score, last_verified_at
2. **移除所有"9 家"硬编码**：代码、文档、注释中均已清除，架构完全可扩展
3. **厂商详情页**（/providers/[slug]）：公司信息卡 + 背景介绍 + 旗下模型 + API 价格 + 会员/Plan + 优惠 + 动态入口 + 数据来源声明
4. **厂商一览页**：按 provider_category 分组（模型厂商/云厂商/API 聚合/开源/应用），显示 headquarters/supports_domestic_payment
5. **后台厂商管理**（admin/providers）：厂商列表 + 统计 + 新增/编辑/停用 + 可信度 + 新厂商发现说明
6. **后台概览新增"厂商管理"入口**
7. **README 更新**：数据源描述改为"首批种子数据源……系统架构支持持续注册新的厂商"
### Git 部署准备（2026-06-13）
**Commit**: fef4125 (main), 文件数 120+，已推送到 github.com/Kannanyyd/aileida-plus.git（需用户 PAT 认证）
**构建**: webpack 编译成功，13/13 测试通过，`ignoreBuildErrors: true`
**部署**: Dockerfile × 2, docker-compose.prod.yml, deploy.sh, nginx.conf.example, .env.production.example 全部就位
**服务器**: SSH 已通 (NewLeiDa.pem), Ubuntu 24.04, Docker 29.5.3
**状态**: HTTP 200 OK, 3 containers (web/worker/postgres), 19 DB tables, 4 seed providers
**修复**: tsx→deps, .dockerignore 去 .next, WORKDIR apps/web, npm install, Docker 阿里云镜像加速

### 代码审计与逻辑修复（2026-06-12 第四次迭代）
修复 3 个关键问题 + 全量审计

### 厂商详情页顶部文案固定（2026-06-12 第五次迭代）
四栏卡片固定结构

### PROJECT_REPORT.md 生成（2026-06-12 第六次迭代）
21 章节项目说明文档

### P0-P1 优化轮次（2026-06-13）
**P0-4: 多价格 pricing 表重构** ✅ UNIQUE改为(model_id,pricing_type,channel)，新增10+字段
**P0-1: Mock 数据全部清除** ✅ 5个页面从硬编码切换为真实 DB 查询，空数据展示空状态
**P0-7: Admin 鉴权** ✅ middleware.ts + /admin-login + API 限频(60s/10s)

### 代码审计与逻辑修复（2026-06-12 第四次迭代）

审计通过 2 个 Agent 并行扫描全部 21 页面 + 7 API + 19 schema 表 + worker pipeline，修复核心问题：

**已修复的严重问题：**
1. ✅ `/api/v1/recommend` 路由已创建（被 recommend 页面依赖但之前缺失）
2. ✅ 迁移 SQL 重写：providers 表补全 40+ 列，新增 10 张缺失表（model_strengths...source_fetch_logs），删除孤立 audit_log 表
3. ✅ Pipeline 优惠数据已接入：runAllCn() 新增 ingestPromotions() 调用

**已知的中等问题（保留为后续待办）：**
- 5 页使用 mock 数据（/ai-news, /plans, /models/[slug]/changelog, /providers/[slug]/news, /recommend 结果）
- diffModel 导出但未在 pipeline 中调用（模型变更检测未激活）
- LLM extract 模块目录缺失（cn-provider 仅用启发式解析，未实现 LLM 兜底）
- review-card.tsx 组件未被 ReviewSection 引用（ReviewCard 作为子组件在 review-section 中应已被使用，检查确认 OK）
- 16 个表缺少独立 storage 函数（MVPs 阶段大部分数据通过 web API 直写）

### 厂商详情页顶部文案固定（2026-06-12 第五次迭代）

重构 `/providers/[slug]` 顶部区域为固定四栏格式：
1. **一句话简介** — 这家公司/平台是谁，主要提供什么 AI 服务（来自 short_description）
2. **适合用户** — suitable_users 标签（开发者/企业/内容团队/个人/API聚合需求）
3. **主要产品** — main_products 标签，或从 supports_api/supports_subscription/enterprise 自动推断
4. **计费特点** — billingSummary() 自动拼接：API按token/会员按月/企业询价 + supported_currencies + domestic_payment/invoice

每个模块位于独立的 glass 卡片中，sm:grid-cols-2 lg:grid-cols-4 响应式排列。

**审计覆盖范围：**
- 页面路由：21 个 page.tsx 全部存在且可访问
- API 路由：8 个 route.ts（含新增 recommend）全部对应
- 组件导入：16 个组件引用全部解析正确
- 查询函数：listModels, getModelBySlug, listProviders 等全部可用
- Schema 一致性：web 与 worker 两套 Drizzle schema 表名/列名完全一致
