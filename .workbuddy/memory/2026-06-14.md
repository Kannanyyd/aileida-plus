# 2026-06-14 工作日志

## 首页 500 修复
- 根因：page.tsx 调 `rank(models, "overall").slice(0,8)`，v3 rank() 返回 `{items,...}` 对象
- 修复：rank() 增加向后兼容 `.slice()`/`.map()`/`.length` 方法
- commit：`cc6865e`
- 线上：从 Docker 镜像启动的容器正常 200（旧代码兼容），新 .next 注入导致 chunk 冲突

## Docker Hub 阻塞确认
- 尝试多次 `docker compose build web --no-cache` → 拉取 node 镜像超时
- 尝试 `.next` tar 注入 → 500（新旧 Webpack chunk ID 不匹配）
- 结论：热修不可靠，必须等 Docker Hub 恢复后重建镜像

## 文档产出
- `docs/HOTFIX_RISK.md`：热修风险 + 恢复步骤
- `docs/PROJECT_PROGRESS_AND_TODO.md`：更新 DNS 修正、P0 列表
- `.workbuddy/memory/MEMORY.md`：更新项目记忆

## 当前线上状态
- `/` `/models` `/providers` `/rankings` `/recommend` → 200
- `/rankings/[type]` `/compare` → 404（源码已有，Docker 未重建）
- containers: web(Up)/worker(Up)/postgres(Healthy)

## GitHub
- 远程 HEAD = `7dd4e35`（用户已手动 push）
- 本地领先 2 commits (`4d9b33b` `docs: hotfix...`)，下次需手动 push
# P1 review_queue / pricing gaps 后台治理完成记录

时间：2026-06-14 20:27 UTC+8

本轮完成了 review_queue / pricing gaps 后台治理。最终代码为 `6f94857`，主功能 commit 是 `e551ee3 feat: add review queue governance`，补丁 commit 是 `6f94857 fix: stabilize domestic pricing gap query`。GitHub 已 push，服务器 `~/aileida-plus` 已同步到 `6f94857`，并完成正式 Docker rebuild、数据库迁移和容器重启。

服务器部署使用本地 `D:\Agent\自动化\AI订阅雷达\NewLeiDa.pem`，默认 `id_ed25519` 对 `ubuntu@175.178.213.71` 不可用。部署命令包括 `docker compose -f docker-compose.prod.yml build web worker --progress=plain`、`docker compose -f docker-compose.prod.yml run --rm web npm run db:migrate`、`docker compose -f docker-compose.prod.yml up -d web worker`。后来发现 `/api/admin/pricing-gaps` 触发旧 SQL 参数化问题，已用 `inArray` 修复并单独 rebuild/up web。

修改范围：
- `apps/web/scripts/migrate.ts`
- `apps/web/lib/db/schema.ts`
- `apps/worker/src/storage/schema.ts`
- `apps/web/middleware.ts`
- `apps/web/lib/admin/review-queue.ts`
- `apps/web/app/admin/review-queue/**`
- `apps/web/app/admin/pricing-gaps/page.tsx`
- `apps/web/app/api/admin/review-queue/**`
- `apps/web/app/api/admin/pricing-gaps/route.ts`
- `apps/worker/src/storage/review-store.ts`
- `apps/worker/src/storage/pricing-store.ts`
- `apps/worker/src/storage/discovery-store.ts`
- `apps/web/lib/db/queries.ts`

数据库变更：
- `review_queue` 新增 `dedupe_key`、`last_seen_at`、`occurrence_count`、`latest_payload`、`latest_error_message`。
- 新增 `review_audit_logs`。
- 新增 pending 去重索引 `review_pending_dedupe_uq`。

新增后台：
- `/admin/review-queue`：支持 reason/provider/canonical_provider/model_family/currency/region/source_provider/selling_platform_provider/status/has_source_url/has_snapshot/confidence_min/created_from/created_to/q 筛选。
- `/admin/review-queue/[id]`：查看 payload、source_url、source snapshot 摘要、已有 pricing、同模型渠道价格；支持 approve pricing、ignore、reject、needs_more_info。
- `/admin/pricing-gaps`：国内 provider 缺口表，包含 models、CNY pricing、missing CNY、review pending、source status、next action。

新增 admin API：
- `GET /api/admin/review-queue`
- `GET /api/admin/review-queue/[id]`
- `POST /api/admin/review-queue/[id]/approve`
- `POST /api/admin/review-queue/[id]/reject`
- `POST /api/admin/review-queue/[id]/ignore`
- `POST /api/admin/review-queue/[id]/needs-more-info`
- `POST /api/admin/review-queue/bulk`
- `GET /api/admin/pricing-gaps`

鉴权：
- `/admin/*` 未登录返回 307 登录跳转。
- `/api/admin/*` 未登录返回 401 JSON。
- `/api/admin/*` 非 GET 有 2 秒基础限流。

review_queue 当前状态：
- 总数：`1233`
- `low-confidence-new-pricing`：486
- `latest-model-missing-pricing`：336
- `multi-source-divergence`：197
- `official-new-model`：172
- `low-confidence`：26
- `possible-deprecated`：13
- smoke test：`admin-smoke-approve/ignore/reject` 各 1 条。
- 当前 pending 旧重复：117 组、158 条重复行。
- 新写入机制已改为 `dedupe_key` upsert；重复抓取只更新 `last_seen_at / occurrence_count / latest_payload / latest_error_message`。
- 未批量删除旧重复，避免误删历史审计数据。

操作闭环验收：
- approve 成功把一条测试 review 写入 pricing。
- ignore 成功置为 ignored。
- reject 成功置为 rejected。
- audit log 写入成功。
- 测试 pricing 已删除，避免污染真实价格；测试 review/audit log 保留用于验收追踪。
- `review_audit_logs` 当前为 4。

国内 pricing gaps 样例：
- alibaba-cloud：models 505，CNY 5，missing 500
- siliconflow：34 / 6 / 28
- moonshot：27 / 3 / 24
- minimax：20 / 0 / 20
- deepseek：19 / 2 / 17
- bytedance-volcano：10 / 0 / 10
- baidu-qianfan：14 / 5 / 9
- tencent-hunyuan：14 / 6 / 8
- zhipu：2 / 0 / 2

页面与日志验收：
- `/`、`/models`、`/models/new`、`/providers`、`/rankings`、`/recommend`、`/compare` 全部 200。
- `/admin`、`/admin/review-queue`、`/admin/pricing-gaps`、`/admin/data-quality` 未登录全部 307。
- `/admin/review-queue`、`/admin/pricing-gaps`、`/admin/review-queue/[id]` 已登录全部 200。
- `/api/admin/review-queue`、`/api/admin/pricing-gaps` 未登录 401；已登录可返回数据。
- `aileida-web` Up，`aileida-worker` Up，`aileida-postgres` Healthy。
- 日志关键字检查未发现 `500 / digest / relation does not exist / tsx not found / EACCES / password authentication failed / server-side exception`。

后续建议：
1. 不处理 DNS / Nginx / HTTPS。
2. 后续如要清理 review_queue 旧重复，先做只读 SQL 审计和迁移方案，不要直接删除。
3. pricing gaps 下一轮优先 MiniMax、火山方舟、智谱、阿里百炼大缺口。
4. 后台 bulk 当前只做状态处理，暂不做批量价格入库，避免误操作。
# 数据一致性复核 + review_queue 旧重复清理

时间：2026-06-14 21:18 UTC+8

## 本轮目标
- 复核 CNY pricing 统计口径，尤其 Kimi / Moonshot 8 条 CNY 在 pricing gaps 中只显示 3 条的问题。
- 修复 pricing gaps 统计口径。
- 非破坏式清理旧 pending review_queue 重复项。
- 增强 `/admin/review-queue` 列表实用性和 approve 安全校验。
- 不扩展新价格源，不处理 DNS / Nginx / HTTPS，不碰 Chromium / Playwright。

## 代码状态
- 功能 commit：`ddd1e00 fix: harden review queue governance`
- 已 push：是，`origin/main = ddd1e00`
- 服务器源码：`~/aileida-plus = ddd1e00`
- 本地 typecheck：通过
- 本地 web build：通过

## 部署状态
- 生产数据库清理已执行。
- 生产 web 镜像未更新到 `ddd1e00`。
- 原因：服务器 Docker build / buildx 连续卡住：
  - `docker compose -f docker-compose.prod.yml build web --progress=plain` 超时
  - `docker compose -f docker-compose.prod.yml build web` 超时
  - `docker build -t aileida-plus-web -f Dockerfile .` 超时
  - `docker builder prune -af` 清空了 31GB build cache，但之后 build 仍卡住
- 已终止挂起 build 进程，线上容器保持上一版正常运行。
- 后续需要先修 Docker build/buildx 状态，才能让 `ddd1e00` 的后台 UI/API 改动上线。

## CNY pricing 数据一致性结论
- 全库当前 CNY pricing 总数：`32`，未减少。
- Kimi / Moonshot 上一轮 8 条 CNY pricing 仍全部存在。
- 根因不是 pricing 被删除，也不是测试清理误删。
- 根因是 pricing gaps 旧查询按 raw model provider 分组，Moonshot 被拆成：
  - `moonshotai`：5 条
  - `moonshot`：3 条
- 按 `model_owner_provider / selling_platform_provider / source_provider` 统一口径后：
  - `moonshot` CNY pricing = 8
- 相关字段例子：
  - `model_owner_provider = moonshot`
  - `selling_platform_provider = moonshot`
  - `source_provider = moonshot`
  - 但部分模型表 provider slug 是 `moonshotai`

## 修复后的 pricing gaps 统计口径
- `domesticPricingGapAudit()` 已改为统一 provider key CTE。
- 模型数量：按 owner/model provider 归属统计。
- CNY pricing：按 owner / selling_platform / source_provider 任一命中统计。
- 支持平台价不漏算：
  - DeepSeek 在硅基流动销售
  - Qwen 在阿里百炼销售
  - Kimi / Moonshot 的 `moonshotai` / `moonshot` 分裂

修复后等价 SQL 统计样例：
| provider | models | CNY pricing | missing CNY |
|---|---:|---:|---:|
| moonshot | 43 | 8 | 35 |
| alibaba-cloud | 529 | 7 | 522 |
| deepseek | 33 | 6 | 27 |
| siliconflow | 34 | 6 | 28 |
| tencent-hunyuan | 16 | 6 | 10 |
| baidu-qianfan | 19 | 5 | 14 |
| bytedance-volcano | 10 | 0 | 10 |
| minimax | 21 | 0 | 21 |
| zhipu | 2 | 0 | 2 |

注意：这部分代码已 push，但因 Docker build 卡住，线上页面/API 暂未运行新口径。

## review_queue 旧重复清理
- 新增脚本：`npm -w web run review:dedupe`
- 生产上已通过 postgres 容器内 SQL 执行同等清理。
- 清理方式：
  - 保留每组信息最完整/较早的一条 pending。
  - 其他重复项标记为 `ignored_duplicate`。
  - 不物理删除。
  - 合并保留项 `occurrence_count / latest_payload / latest_error_message / last_seen_at / dedupe_key`。
  - 写入 `review_audit_logs`。
- 清理前：
  - pending 重复组：117
  - pending 重复行：158
- 清理后：
  - pending 重复组：0
  - pending 重复行：0
  - `ignored_duplicate`：158

## review_queue 后台增强
- `/admin/review-queue` 代码已增强：
  - 默认 high impact 排序：
    - pricing-conflict
    - multi-source-divergence
    - low-confidence-new-pricing
    - latest-model-missing-pricing
    - domestic-price-missing
  - 支持排序：
    - occurrence_count desc
    - confidence desc / asc
    - created_at desc
    - last_seen_at desc
  - 列表新增展示：
    - occurrence_count
    - dedupe_key
    - provider
    - canonical_provider
    - model
    - currency
    - region
    - source_url
    - confidence
    - last_seen_at
  - 新增批量操作：
    - bulk ignore duplicates
    - bulk mark needs_more_info
    - bulk reject suspicious
- 注意：这部分 UI 代码未上线，原因同上：Docker build 卡住。

## approve 安全校验增强
- approve pricing 前新增校验：
  - `currency_native` 必填
  - `region` 必填
  - `source_url` 必填
  - `input_price / output_price` 至少一个存在
  - `billing_unit` 必填
  - CNY 必须保留 native CNY 价格
  - 同 model + currency + region + channel + selling_platform_provider 若存在不同 source 的 pricing，默认拒绝并要求显式确认
  - approve 后继续写 `review_audit_logs`
- 注意：代码已 push，未上线。

## 当前生产验收
- `/`：200
- `/models`：200
- `/models/new`：200
- `/providers`：200
- `/rankings`：200
- `/recommend`：200
- `/compare`：200
- `/admin`：307
- `/admin/review-queue`：307
- `/admin/pricing-gaps`：307
- `/api/admin/review-queue`：401
- `/api/admin/pricing-gaps`：401
- `aileida-web`：Up
- `aileida-worker`：Up
- `aileida-postgres`：Healthy
- 日志关键字未发现：`500 / digest / relation does not exist / tsx not found / EACCES / password authentication failed / server-side exception`

## 后续接手建议
1. 先处理 Docker build/buildx 卡住问题，不要继续扩展价格源。
2. 不建议用 `.next` 或 docker cp 热修上线 `ddd1e00`，否则又会回到热修风险。
3. Docker build 恢复后执行：
   - `docker compose -f docker-compose.prod.yml build web --progress=plain`
   - `docker compose -f docker-compose.prod.yml up -d web`
   - 验证 `/admin/review-queue` 和 `/admin/pricing-gaps` 已登录 200
   - 验证 `/api/admin/pricing-gaps` 中 `moonshot` CNY pricing 为 8
4. review_queue 旧重复已清理，不要重复清理；如再清理，先 dry-run。
# Docker build / buildx 卡住修复并正式上线

时间：2026-06-14 22:22 UTC+8

## 本轮目标
- 暂停新功能、价格源、HTTPS/DNS/Nginx。
- 修复服务器 Docker build/buildx 卡住问题。
- 让 `ddd1e00` 的 review_queue / pricing gaps 后台加固正式 rebuild 并上线。
- 不使用 `.next` 热修，不使用 `docker cp` 热修。

## 根因
- Docker daemon、buildx、磁盘、内存整体正常。
- 磁盘：`/` 约 59G，总可用约 41G。
- 内存：3.6GiB，总可用约 2.5GiB，swap 约 1.8GiB 可用。
- Docker build cache 曾达到约 31GB，后已清空；但清空 cache 不是根因。
- Docker 日志显示构建实际进入 `[7/10] RUN npm install --include=dev` 后长时间执行；我们之前的超时取消导致 BuildKit session 报 `only one connection allowed` / `context canceled`。
- web `Dockerfile` 没有复制 `package-lock.json`，且使用 `npm install --include=dev`，清空 cache 后必须重新解析 workspace dependency，安装阶段很慢且进度不清晰。
- worker `Dockerfile.worker` 已经使用 `package-lock.json + npm ci`，web 与 worker 不一致。

## 修复
- commit：`2c64635 fix: make web docker install deterministic`
- 修改 `Dockerfile`：
  - `COPY package.json package-lock.json tsconfig.base.json ./`
  - `RUN npm ci --production=false`
- 已 push 到 GitHub。
- 服务器源码已同步到 `2c64635`。
- 未动 postgres volume。
- 未执行 `docker system prune -a --volumes`。
- 曾执行 `docker builder prune -af` 清理 build cache；只清 builder cache，不清 volume。

## 正式部署
- 已执行：
  - `docker compose -f docker-compose.prod.yml build web --progress=plain`
  - `docker compose -f docker-compose.prod.yml up -d web`
- web 镜像已正式 rebuild：
  - 新 web image/container ID 均为 `sha256:3c720ae7...`
- worker 未 rebuild：
  - 本轮代码只改 web Dockerfile，worker 运行镜像仍等于 latest image ID。
  - worker 容器正常运行。

## 构建结果
- `npm ci` 首次无缓存安装耗时约 8 分钟，但有明确输出并成功完成。
- `next build` 成功。
- 未使用热修。

## 上线验收
- 页面：
  - `/`：200
  - `/models`：200
  - `/models/new`：200
  - `/providers`：200
  - `/rankings`：200
  - `/recommend`：200
  - `/compare`：200
  - `/admin`：307
  - `/admin/review-queue` 未登录：307
  - `/admin/pricing-gaps` 未登录：307
  - `/admin/data-quality` 未登录：307
- API：
  - `/api/admin/review-queue` 未登录：401
  - `/api/admin/pricing-gaps` 未登录：401
  - 已登录 `/api/admin/review-queue?limit=3`：可用
  - 已登录 `/api/admin/pricing-gaps`：可用

## ddd1e00 功能上线确认
- `/admin/review-queue` HTML 中已确认：
  - `Bulk ignore duplicates`
  - `Needs more info`
  - `Reject suspicious`
  - `Dedupe`
  - `Source`
  - `Last seen`
  - `confidence desc`
  - `high impact`
- approve 必填校验已生效：
  - 对缺 `currency_native` 的测试 review 调 approve，返回 400：`currency_native is required before approve`
- `/api/admin/pricing-gaps` 中：
  - `moonshot`：models 43，CNY pricing 8，missing 35
  - `alibaba-cloud`：529 / 7 / 522
  - `deepseek`：33 / 6 / 27
  - `siliconflow`：34 / 6 / 28
- CNY pricing 总数仍为 32。
- pending duplicate groups：0。
- pending duplicate rows：0。

## 容器和日志
- `aileida-web`：Up，使用最新 web image。
- `aileida-worker`：Up。
- `aileida-postgres`：Healthy。
- 日志关键字未发现：
  - `500`
  - `digest`
  - `relation does not exist`
  - `tsx not found`
  - `EACCES`
  - `password authentication failed`
  - `server-side exception`
  - `rank(...).slice/map`

## 后续注意
- 后续 build 第一次可能仍会花数分钟跑 `npm ci`，但现在是确定性安装，有明确进度。
- 不要再改回 `npm install`。
- 不要使用 `.next` / docker cp 热修。
- 可以继续下一阶段前，先以 `/api/admin/pricing-gaps` 的 `moonshot=8` 作为统计口径上线验收点。
## 2026-06-14 国内 CNY 价格第三轮补全（进行中）

- 用户要求：继续扩大国内原生人民币价格覆盖，将 CNY pricing 从 32 提升到至少 60，优先 80+；不处理 DNS/Nginx/HTTPS，不硬卡 Chromium/Playwright，不做大量人工录价。
- 已做本地代码：
  - `apps/worker/src/sources/cn-cny-pricing.ts`
    - 扩展 SiliconFlow：DeepSeek/Kimi/GLM/MiniMax/Qwen 更多模型。
    - 扩展 Aliyun Bailian：Qwen、DeepSeek、GLM、Kimi、MiniMax 平台价。
    - 新增 MiniMax 官方价格源。
    - 新增 Zhipu 官方价格页 JS 解析源。
    - 新增 Volcengine/Doubao 官方文档审计源，当前不入正式价格。
    - 新增 ModelScope 官方文档审计源，当前不入正式价格。
  - `apps/worker/src/pipeline.ts`
    - 注册新增 CNY 源。
    - 新增 lowercase `minimax` provider metadata。
- 本地 fetcher 预检：
  - SiliconFlow 19
  - Aliyun Bailian 37
  - MiniMax 11
  - Zhipu 8
  - Volcengine/Doubao 0（snapshot only）
  - ModelScope 0（snapshot only）
- 本地验证：`npm run typecheck` 通过。
- 注意：
  - 火山方舟官方文档响应中有 Doubao 价格单元格，但列顺序需要人工确认，暂不入正式 pricing，避免错误价格污染。
  - ModelScope 文档未发现稳定 token 单价，不把免费额度当 API 单价。
  - 下一步需 commit/push、服务器 rebuild worker、运行 CNY crawler、验收 CNY 总数和页面/API。

### 生产结果

- commit：`7e20a1e feat: expand domestic cny pricing sources`，已 push。
- 服务器源码：`7e20a1e`。GitHub fetch 在服务器卡住，已停止卡住进程；通过 git bundle 上传后 `git merge --ff-only FETCH_HEAD` 正式同步。
- worker 已正式 rebuild/up，未使用热修。
- 已运行 `crawl:cny-pricing`。
- CNY pricing：32 -> 94。
- 新覆盖：
  - MiniMax 官方：11。
  - Zhipu GLM 官方 JS：8。
  - Aliyun Bailian 扩展：37。
  - SiliconFlow 扩展：19。
  - Volcengine/Doubao：fetch log/snapshot only，列顺序未确认，不入正式价。
  - ModelScope：fetch log/snapshot only，未发现稳定 token 单价。
- 国内榜单：
  - domestic Top20：18 个 CNY 标记。
  - frontier-value china_mainland：12 条中 6 个 CNY 标记。
- 推荐：
  - 国内写作场景 `relaxedFilters=[]`。
  - budget/balanced/premium 三组 Top5 均为 CNY。
  - `pricingGapAlerts` 正常。
- 页面/API：
  - `/`、`/models`、`/models/new`、`/providers`、`/rankings`、`/recommend`、`/compare` 为 200。
  - `/admin`、`/admin/review-queue`、`/admin/pricing-gaps` 未登录 307。
  - `/api/admin/review-queue`、`/api/admin/pricing-gaps` 未登录 401。
- 日志：未见关键错误匹配。
- 注意：pending duplicate groups 当前查到 1；本轮未清理 review_queue，应在下一轮数据治理复核。
## 2026-06-14 public trust / SEO pre-launch pass

- Final deployment result:
  - Code commit `2ae6d85` was pushed and deployed.
  - Server source is `2ae6d85`.
  - Web image was formally rebuilt and restarted; no hotfix.
  - Worker was not rebuilt because this was web/UI/docs only.
  - Smoke pages all passed: `/`, `/models`, `/models/new`, `/models/deepseek-chat`, `/models/kimi-k2.6`, `/models/ernie-5.1`, `/providers`, `/rankings`, `/rankings/domestic`, `/rankings/frontier-value`, `/recommend`, `/compare`, `/robots.txt`, `/sitemap.xml` = 200.
  - Admin unauthenticated redirects: `/admin`, `/admin/review-queue`, `/admin/pricing-gaps` = 307.
  - Admin APIs unauthenticated: `/api/admin/review-queue`, `/api/admin/pricing-gaps` = 401.
  - Recommend domestic writing smoke: 200, `relaxedFilters=[]`, `pricingGapAlerts=5`, `latestModelAlerts=6`.
  - review_queue duplicate state: `pending_null_dedupe=0`, `pending_duplicate_groups=0`.
  - web/worker logs had no critical error matches.

- Current task: public page experience, data trust display, and SEO basics. Do not touch DNS/Nginx/HTTPS, Chromium/Playwright, or new pricing source expansion.
- review_queue duplicate check:
  - The apparent duplicate group was caused by `dedupe_key is null` historical rows being grouped as one bucket.
  - Effective dedupe key query found 0 real duplicate groups.
  - Production DB backfilled 896 pending `dedupe_key` values and inserted 896 `review_audit_logs` records.
  - No physical deletion; pending duplicate groups now 0.
- Code changes in progress/done:
  - Added `PriceValue`, `PriceSourceBadges`, and `SourceLink` in `apps/web/components/price-trust.tsx`.
  - Updated `ModelCard`, rankings list/detail pages, `/models`, `/models/[slug]`, `/recommend`, `/compare`, homepage, global metadata, robots, and sitemap.
  - Displays now distinguish native CNY, native USD, and estimated currency; show source URL, update time, confidence, channel type, and data quality flags where available.
  - `/models/[slug]` shows owner/selling/source provider and multi-channel pricing rows.
  - `/recommend` shows advisor-style reasons, stronger/cheaper alternatives, pricing gap alerts, and latest unpriced model alerts.
- Local validation:
  - `npm run typecheck` passed.
  - `npm -w web run build` passed.
- Caveat:
  - Several existing TSX pages had corrupted Chinese text. The affected rewritten pages now use ASCII-safe copy to avoid more encoding damage. Chinese copy should be restored later through a safer UTF-8 workflow.
