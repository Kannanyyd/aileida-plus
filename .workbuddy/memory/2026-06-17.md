# 2026-06-17

## Obsolete Model Exclusion

- User decision: models that are long outdated should be removed from homepage current/default surfaces instead of merely demoted.
- Main issue observed before fix: homepage official-current module could still show `DeepSeek Reasoner` / `deepseek-r1`, which makes the site look outdated even after default ranking filters were tightened.
- Code changes:
  - `packages/pricing-core/src/official-current/index.ts`
    - `deepseek-reasoner` / `deepseek-r1` changed from `recommended` + `homepageEligible: true` to `previous` + `homepageEligible: false`.
  - `apps/web/app/page.tsx`
    - Added defensive obsolete-model filtering for homepage official-current rows.
  - `apps/worker/src/cli/sync-official-current.ts`
    - Preserves `previous_generation` / `deprecated` status when syncing official-current entries to candidates/models.
- Validation passed:
  - `npm run typecheck`
  - `npm -w web run build`
- Production follow-up:
  - Commit/push/deploy.
  - Run `npm run sync:official-current` in production/worker so DB catalog updates the DeepSeek R1 row.
  - Verify homepage official-current no longer includes DeepSeek Reasoner / deepseek-r1.
