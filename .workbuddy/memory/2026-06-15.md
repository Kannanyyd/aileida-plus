# 2026-06-15 P0 data freshness and homepage Top8 repair

## User request

- User complained the homepage looks amateur and the copy is weak.
- The attached `D:\Desktop\下一步.txt` explicitly says to pause broad Chinese copy polish and UI redesign because a more serious issue was found: the public homepage selected Top8 contained long-stale models.
- This round should only do:
  - data freshness audit
  - homepage selected ranking repair
  - stale/superseded filtering
  - 12h freshness mechanism
- Do not do DNS/Nginx/HTTPS, Chromium/Playwright, large new pricing source expansion, or commercialization.

## Production baseline before changes

- Server source: `f9f4317`.
- Containers: web, worker, postgres up.
- Existing production API Top8 was better than the homepage card data, but homepage cards still showed old/aggregator-ish selections because the page rehydrated ranked slugs through model records.
- Production source/pricing logs were recently checked, so the main bug is not crawler recency only; it is that homepage/ranking/recommendation did not enforce freshness and supersession strongly enough.

## Local changes implemented

- `apps/web/lib/db/queries.ts`
  - Added derived freshness fields to model records:
    - `source_checked_at`
    - `pricing_checked_at`
    - `official_source_checked_at`
    - `freshness_status`
    - `source_age_hours`
    - `pricing_age_hours`
    - `model_age_days`
  - Added derived supersession fields:
    - `has_newer_family_model`
    - `superseded_by_model_id`
    - `is_current_default_pick`
  - Enriches models from `source_fetch_logs`, `model_discovery_logs`, and `latest_model_candidates`.
  - Added `dataFreshnessOverview()` for homepage freshness cards.

- `apps/web/lib/rank/score.ts`
  - Adds `hideStale`, `hideSuperseded`, `maxSourceAgeHours`, `homepageStrict`.
  - Ranking filters stale and superseded models by default.
  - Homepage strict mode filters suspicious/manual-review/missing-source/low-confidence stale candidates.
  - Ranking API items now include freshness/supersession fields and `why_ranked`.

- `apps/web/app/api/v1/rankings/[type]/route.ts`
  - Added query params:
    - `max_source_age_hours`
    - `hide_stale`
    - `hide_superseded`

- `apps/web/app/api/v1/recommend/route.ts`
  - Default non-cheapest recommendations filter superseded models and stale data over 72h.
  - Recommendation payload includes freshness/supersession fields.

- `apps/web/app/page.tsx`
  - Homepage title/copy tightened into a more professional API pricing intelligence positioning.
  - Homepage Top8 now uses strict 12h freshness and current-main-model ranking.
  - Adds freshness cards for model discovery, pricing source, and CNY pricing updates.

- `apps/worker/src/cli/audit-freshness.ts`
  - New command for source/pricing freshness audit.

- Scripts:
  - root `audit:freshness`
  - root `freshness:audit`
  - root `crawl:pricing`
  - worker `audit:freshness`
  - worker `crawl:pricing`

## Local validation

- `npm run typecheck` passed.
- `npm -w web run build` passed.
- `npm -w worker run build` passed.
- `npm run audit:freshness` starts, but local Postgres is not running at `127.0.0.1:5432`, so it fails locally with `ECONNREFUSED`.

## Next required steps

1. Commit and push.
2. Sync server source.
3. Formally rebuild web and worker images.
4. Run `npm run audit:freshness` in the production worker context.
5. Validate public pages, admin redirects/API auth, ranking freshness fields, recommendation stale filtering, and logs.

## Completed production result

- Code commits:
  - `cecb0c6 fix: enforce homepage freshness ranking`
  - `7bd0b39 fix: include latest candidates in freshness audit`
  - `decef06 fix: audit actual homepage freshness ranking`
- GitHub pushed and server source synced to `decef06`.
- Web image formally rebuilt and restarted for homepage/ranking/recommendation changes.
- Worker image formally rebuilt and restarted for `audit:freshness`.
- No `.next` hotfix and no `docker cp`.
- Production `audit:freshness`:
  - Source freshness: 23 sources.
  - `stale_over_12h=0`, `stale_over_24h=0`.
  - Latest source check: `2026-06-14 16:05:05 UTC`.
  - Latest pricing check: `2026-06-14 16:04:47 UTC`.
  - CNY pricing count: 94.
  - Ranking API homepage Top8 all fresh, all current_frontier/current_mainstream, no superseded models.
- Homepage Top8 after fix:
  - `kimi-k2.7-code`
  - `gemini-flash-latest`
  - `gemini-pro-latest`
  - `mimo-v2.5`
  - `minimax-m3`
  - `grok-4.20`
  - `openrouter/xiaomi/mimo-v2.5`
  - `claude-opus-4.8`
- Domestic Top8 sample: all native CNY, china_mainland, fresh.
- Recommend domestic writing smoke: `relaxedFilters=[]`; balanced Top5 are fresh, CNY-priced, and not superseded.
- Pages:
  - `/`, `/models`, `/models/new`, `/providers`, `/rankings`, `/rankings/domestic`, `/rankings/frontier-value`, `/recommend`, `/compare` returned 200.
  - `/admin`, `/admin/review-queue`, `/admin/pricing-gaps`, `/admin/data-quality` returned 307 unauthenticated.
  - Admin APIs returned 401 unauthenticated.
- Logs had no critical matches.

---
## P0 official-current coverage + homepage Top8 currentness repair

### User request

- Latest `D:\Desktop\下一步.txt` says the previous freshness repair is not enough.
- Current problem: homepage Top8 still contains models that look several versions behind or lack official-current evidence.
- This round must not do broad Chinese copy polish, SEO expansion, DNS/Nginx/HTTPS, Chromium/Playwright, new price sources, or commercialization.

### Baseline finding

- Production source before this round: `b107cc5`.
- `npm run audit:freshness` in production showed source logs were fresh, but Top8 still contained questionable homepage picks:
  - `mimo-v2.5`
  - `openrouter/xiaomi/mimo-v2.5`
  - `grok-4.20`
  - `gemini-flash-latest` / `gemini-pro-latest` mostly relied on source freshness/latest alias.
- Conclusion: source freshness was being used as a proxy for model currentness.

### Local changes prepared

- Added official current/recommended/latest catalog:
  - `packages/pricing-core/src/official-current/index.ts`
  - Exported from `packages/pricing-core/src/index.ts`.
- Split freshness:
  - `source_freshness_status`
  - `model_recency_status`
  - `is_official_current`
  - `is_official_recommended`
  - `official_current_*`
- Homepage strict ranking now requires official-current evidence.
- Ranking API gained:
  - `homepage_strict`
  - `require_official_current`
- New audit commands:
  - `npm run audit:official-current`
  - `npm run audit:homepage-currentness`

### Local validation

- `npm run typecheck` passed.
- `npm run build` passed.
- `tsconfig.tsbuildinfo` files were restored and are not part of this change.

### Next required steps

1. Commit/push.
2. Sync production source.
3. Formally rebuild web and worker.
4. Run new audits in production.
5. Verify pages/admin/logs.

### Completed production result

- Final code commit: `7a448fb`.
- Web was formally rebuilt/restarted for homepage official-current filtering.
- Worker was formally rebuilt/restarted for the new audit commands.
- New commands verified in production:
  - `npm run audit:homepage-currentness`
  - `npm run audit:official-current`
- `audit:homepage-currentness` strict result:
  - all Top8 rows have official-current catalog evidence and official source URL.
  - no previous/stale/unknown rows.
  - no superseded rows.
  - no source-fresh-but-model-not-current rows.
- Strict homepage Top8 after fix:
  - `minimax-m3`
  - `gemini-flash-latest`
  - `kimi-k2.7-code`
  - `grok-4.20`
  - `claude-opus-4.8`
  - `kimi-k2.6`
  - `gemini-3.5-flash`
  - `gpt-5.5`
- Old/non-evidence rows no longer pass strict homepage filtering:
  - `mimo-v2.5`
  - `openrouter/xiaomi/mimo-v2.5`
  - `gpt-5-codex`
- Official current audit gaps:
  - Missing DB entries: `llama-4-maverick`, `llama-4-scout`, `command-r-plus-08-2024`, `north-mini-code-1-0`, `doubao-seed-1.6`, `glm-4.6`.
  - In DB but no pricing: `mistral-medium-3.5`.
- Smoke test:
  - Public pages returned 200.
  - Admin pages returned 307 unauthenticated.
  - Admin APIs returned 401.
  - web/worker/postgres are up; postgres healthy.
  - Logs had no critical matches.

---
## System logic audit / architecture freeze

### User request

- Latest `D:\Desktop\下一步.txt` asks to stop all new feature development and produce a clear system logic/data-flow audit.
- Do not work on DNS, Nginx, HTTPS, Chinese copy, SEO, new price sources, UI redesign, commercial features, or hardcoded new model stuffing.

### Work completed

- Added `docs/SYSTEM_INVARIANTS.md`.
- Added `docs/SYSTEM_LOGIC_AUDIT.md`.
- Updated project progress and memory handoff docs.

### Production audit facts captured

- Stored `current_frontier`: 15 rows.
- Stored `current_mainstream`: 26 rows.
- Stored `current_mainstream` with pricing: 0.
- Stored `current_mainstream` with `needs_pricing_review=true`: 26.
- Pricing sources stale over 12h: 0.
- Current strict homepage Top8 is safe by runtime official-current catalog audit, but alias/family cleanup remains.

### Main conclusion

- The system has two distinct currentness layers:
  - stored DB lifecycle tier
  - runtime official-current/model-recency enrichment
- Future agents must not use `models.lifecycle_tier` alone as homepage/recommendation truth.
- `freshness_status` is legacy/ambiguous; future API work should prefer `source_freshness_status` and `model_recency_status`.

### Recommended next work

1. Add alias-level dedupe for official catalog equivalents.
2. Insert missing official-current models as price-pending candidates.
3. Move official-current catalog governance from code into DB/admin.
4. Deprecate misleading API fields.

---
## Official-current catalog stabilization phase 1

### User request

- Do not do DNS/Nginx/HTTPS, Chinese copy/SEO, new price sources, UI redesign, or commercial features.
- Stabilize official-current catalog only:
  - alias-level dedupe for Gemini/Grok.
  - insert missing official-current models into candidates/review_queue.
  - add DB-backed official-current catalog tables and read-only admin pages.
  - deprecate misleading `freshness_status`.

### Local changes prepared

- Removed fuzzy family fallback from `findOfficialCurrentModel()`.
- Added explicit xAI alias mapping: `grok-4.20 -> grok-4-0709`.
- Added DB catalog schema/migration:
  - `official_current_models`
  - `official_model_aliases`
  - `official_catalog_runs`
- Web ranking/enrichment now reads DB catalog first and marks code catalog as `code-fallback`.
- Homepage/ranking diversity now dedupes by `official_current_model_slug` before raw model family.
- Added read-only admin page:
  - `/admin/official-current`
  - enhanced `/admin/model-aliases`.
- Added worker command:
  - `npm run sync:official-current`
- Added audit additions:
  - `audit:homepage-currentness` checks alias duplicate representatives, Gemini/Grok dedupe, alias review leakage, and deprecated freshness marker.
  - `audit:official-current` reads DB catalog when present and prints Gemini/Grok alias mapping.
  - `audit:freshness-fields` scans web app/components for forbidden direct `freshness_status` UI usage.

### Local validation

- `npm -w @pricing/core run typecheck` passed.
- `npm -w web run typecheck` passed.
- `npm -w worker run typecheck` passed.
- `npm run audit:freshness-fields` passed.
- `npm run build` passed.
- `npm -w worker run build` passed.
- Local `npm run db:migrate` could not run because local PostgreSQL was not listening on 127.0.0.1:5432. Production migration still required.

### Pending before handoff complete

1. Commit and push.
2. On production: `git pull`, `npm run db:migrate`, `npm run sync:official-current`.
3. Rebuild web and worker images formally.
4. Validate `/admin/official-current` and `/admin/model-aliases` unauthenticated 307.
5. Run `npm run audit:homepage-currentness`, `npm run audit:official-current`, `npm run audit:freshness-fields`.
6. Smoke test public pages/admin APIs/logs.

### Production completion for official-current catalog stabilization phase 1

- Code commit deployed: 4fc15ee (`fix: stabilize official current catalog`).
- GitHub push: done.
- Server source was pulled to 4fc15ee before rebuild.
- Production migration executed inside compose web container: success.
- Production sync executed inside compose worker container: success.
  - official_current_models: 27
  - official_model_aliases: 54
  - official_catalog_runs: 1
  - latest_model_candidates from official-current catalog: 27
  - new pending review_queue rows: 7
    - official-current-model-missing: 6
    - official-current-price-missing: 1
- Missing official-current models are now represented in candidates/review_queue; `mistral-medium-3.5` is price pending.
- Pending duplicate groups after sync: 0.
- Gemini alias mapping is explicit:
  - `gemini-flash-latest -> gemini-3.5-flash`
  - `gemini-pro-latest -> gemini-3.1-pro-preview`
  - broad ambiguous aliases such as `gemini-latest` are `needs_alias_review=true` and `homepage_eligible=false`.
- Grok alias mapping is explicit:
  - `grok-4.20 -> grok-4-0709`
  - `grok-4-latest -> grok-4-0709`
  - fast/reasoning/non-reasoning ambiguous variants are `needs_alias_review=true` and `homepage_eligible=false`.
- Web and worker images were formally rebuilt and restarted.
- Page smoke test:
  - `/`, `/models`, `/models/new`, `/providers`, `/rankings`, `/rankings/frontier-value`, `/recommend`, `/compare`: 200.
  - `/admin`, `/admin/official-current`, `/admin/model-aliases`, `/admin/review-queue`, `/admin/pricing-gaps`: 307 when unauthenticated.
  - `/api/admin/review-queue`, `/api/admin/pricing-gaps`: 401 when unauthenticated.
- Audits passed in production:
  - `npm run audit:homepage-currentness`
  - `npm run audit:official-current`
  - `npm run audit:freshness-fields`
- Logs checked after rebuild: no `500`, `digest`, `relation does not exist`, `tsx not found`, `EACCES`, `password authentication failed`, `server-side exception`, or `rank(` critical matches.
- Notes:
  - direct host `npm run db:migrate` failed because host env did not match compose DB auth/Docker DNS; final successful migration used `docker compose run --rm web npm run db:migrate`.
  - no `.next` hotfix or `docker cp` hotfix was used.
